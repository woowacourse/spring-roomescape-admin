spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  # DB connection setting
  datasource:
    url: jdbc:h2:mem:database
    username: sa
    password: password
  # DB initialization
  sql:
    init:
      mode: embedded
      schema-locations: classpath:schema.sql
