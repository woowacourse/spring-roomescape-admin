## 학습 로그 #3

**시간**: 26/04/29 15:06 ~ 17:50 (약 164분)
**학습 범위**: 3단계 시간 관리

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)
- [ ] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [x] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다
  - Reservation에 ReservationTime 객체가 들어갈 시 구현흐름은 알겠으나 코드를 적용하는 것이 어렵다.
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
  - 코드 구현 중에도 i+1 원칙을 도입하여 작은 문제부터 해결하는 전략이였고 실제로 실행하였습니다.
  
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.
  ```aiignore
  이번에 막힌 부분은 Reservation에 ReservationTime 객체가면서 각 기능들의 내부구현을 변경했어야 했는데
  어떻게 흘러가는 지 흐름은 알겠으나 코드로 적용하는 것이 어려운 점이였다.
  따라서 AI에게 다음과 같은 어려움이 있는데 이를 구현하기 위한 예제코드를 달라하여 예제코드를 보며 어떻게 코드가 변경되어야하는지 확인을 했고,
  i+1 원칙을 의식하여 createReservation 메서드를 먼저 변경을 시도 해보았고, 이후엔 감을 잡아서 다른 메서드들은 AI 없이 변경하였습니다.
  ```  

### 3. 전략 평가
- 효과적이었던 것과 그 이유
  ```aiignore
  코드 구현 중에도 i+1 원칙을 의식하여 요구사항을 구현하기 위한 예제 코드 중 한 가지만 어떻게 쓰는 지 확인하였고,
  스스로 구현할 때 동안 예제 코드를 보고 구현해보고 막히는 부분이 있으면 다시 예제코드를 보는 과정을 반복하였고
  기능이 완성되었을 때 다시 지우고 스스로 다시 구현해 보았습니다.
  그 결과 다른 코드들은 AI의 도움없이 완성할 수 있었습니다.
  ```
- 비효과적이었던 것과 그 이유
  ```aiignore
  전 단계(log-02.md)에서 AI 에게 제안받은 내용 중 "3. 10~15분 안에 실험"이 비효율적이였던 것 같습니다.
  구현이 느릴뿐 제가 충분히 생각하고 구현 할 수 있는 것인데 단지 시간이 지났다고해서 AI에게 도움을 받는 것은 아쉽다고 판단했기 때문입니다. 
  ```
- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?
  - i+1 원칙을 통해 한 가지 기능을 스스로 구현해보는 것을 체험해보니, 다른 기능들을 스스로 완성시킬 수 있어 궁합이 좋았다고 판단했습니다.

### 4. AI 피드백
```
제안: 시간 기준 → “진전 여부 기준”으로 변경

- ❌ 10~15분 지나면 무조건 AI 사용
- ⭕ 10~15분 동안 “진전이 있는지”를 기준으로 판단

👉 진전 기준:
- 코드 한 줄이라도 새롭게 작성함
- 다른 접근을 시도함
- 에러를 기반으로 새로운 가설을 세움

👉 진전 없음 기준:
- 같은 코드를 반복해서 보고 있음
- 같은 생각만 반복하고 있음

→ 진전이 없다고 판단되면 AI를 활용한다
```

### 5. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
  ```
  핵심 기능 하나를 직접 구현한 후 나머지 코드로 확장하는 방식은 유지하겠습니다.

  -> 한 번의 구현 성공 경험을 기반으로 다른 코드까지 확장할 수 있었고,
  이를 통해 AI 없이도 전체 구현을 완성할 수 있었기 때문입니다.
  ```

- 바꿀 것과 그 이유
  ```aiignore
  스스로 실험하는 과정에서 "모르는가"를 기준으로 판단하지 않고
  "진전이 있는가"를 기준으로 AI 사용 여부를 판단한다.
  
  -> 알고 있다고 생각해도 진전이 없다면 실제로는 해결이 어려운 상태이기 때문에 이 경우에는 AI를 활용하여 빠르게 구현으로 복귀하기 위함입니다.
  ```
