# 1단계 요구사항 정리

방탈출 카페 관리자가 전화·현장 예약을 직접 등록·관리하는 상황에 필요한 예약 관리 API를 만든다

## 사용자 요구사항

- 관리자는 예약을 추가할 수 있다.
- 관리자는 예약을 조회할 수 있다.
- 관리자는 예약을 삭제할 수 있다.

## 시스템 요구사항

#### 데이터 저장 및 관리

- 별도의 외부 데이터베이스를 연결하지 않고 In-Memory 상에서 동작하도록 한다.
- 애플리케이션의 메모리는 List, AtomicLong으로 구현한다.
- 예약의 ID는 동시성을 고려하여 AtomicLong을 기반으로 순차적으로 부여한다.
- 데이터는 영속성을 가지지 않고, 서버 재시작시 모두 초기화된다.

#### 통신

- 별도의 프론트엔드는 구현하지 않는다.
- 데이터의 교환 포맷은 JSON을 사용한다.
- HTTP 기반의 REST API를 통해 통신한다.

#### 구현 관점

- 이름, 시간, 날짜를 담을 하나의 Reservation 객체가 필요하다.
- 이름, 시간, 날짜를 각각의 객체로 설정하여 정상 형식인지 검증한다.

#### 요구사항 정리

- 사용자 이름은 최대 10자이다.
- 날짜의 형식은 YYYY-MM-DD 이다.
- 시간은 HH:MM 이다.
- 해당 형식이 맞지 않는 경우 예외를 발생한다.

#### API

예약 조회 GET `/reservations`

- 요청: X
- 응답: `[{id, name, date, time}, ...]`

예약 추가 POST `/reservations`

- 요청: `{name, date, time}`
- 응답: `{id, name, date, time}`

예약 삭제 DELETE `/reservations/{id}`

- 요청: X
- 응답: `200 OK`

# 2단계 요구사항 정리

1단계에서 H2 Database를 붙인다.

#### 요구사항 정리

- 애플리케이션의 List<Reservation>, AtomicLong을 제거한다.
- 1단계에서 만든 조회, 추가, 삭제 API를 JdbcTemplate기반으로 전환한다.
- 예약 추가 시 DB가 생성한 id를 응답에 담는다

# 3단계 요구사항 정리

시간 관리 기능을 추가한다.

#### 사용자 요구사항

- 관리자는 예약의 시간을 추가할 수 있다.
- 관리자는 예약의 시간을 조회할 수 있다.
- 관리자는 예약의 시간을 삭제할 수 있다.

#### 구현 요구사항

- 각 예약은 시간과 연결되어야 한다.

#### 더 알아보아야 하는 것 정리

- 예외를 catch 할 때 어떤 예외 타입으로 잡아야 하는가?
- 도메인 예외, Repository 예외, Controller 예외 응답 변환은 각각 어느 계층에서 책임지는 것이 좋은가?
- `@ControllerAdvice`와 `@ExceptionHandler`는 어떤 상황에서 도입하는 것이 적절한가?
- DAO와 Repository의 차이는 무엇일까?
- 요청/응답 형태와 도메인 형태가 다를 때 변환 책임은 Controller, DTO, Domain 중 어디에 두는 것이 좋은가?
- 정적 팩토리 메서드는 언제 생성자보다 좋을까?
- Jackson 직렬화/역직렬화는 어떻게 동작하는가?
- JSON 형태와 객체 형태가 다를 때 `@JsonValue`, `@JsonCreator` 등으로 어떻게 사용하는지?
- `JdbcTemplate`은 내부적으로 SQL 실행 결과를 어떻게 객체로 매핑하는가?
- `(ResultSet, rowNum) -> {}`는 어떻게 동작할까?
- `KeyHolder`는 DB가 생성한 id를 어떻게 가져오는가?
- H2 인메모리 DB에서 `DB_CLOSE_DELAY=-1`은 어떤 의미이고 테스트에서는 어떻게 사용되어지는가
- 테스트마다 DB 상태를 격리하려면 스키마 생성, 데이터 삭제, `@DirtiesContext` 등을 어떻게 선택할까?
- AtomicLong vs Long의 차이
