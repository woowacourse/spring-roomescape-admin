## 학습 로그 #1

**시간**: 04/28  13:00 ~  16:00 (약 180분)
**학습 범위**: 1단계: MVC

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가?
- [ ] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [x] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
  - 학습법에서 계획한 방향과 비슷하게 실행했다. AI에게 내가 어떻게 할 것인지를 실행하기 전 검토를 받고, 진행했다. 과한 설계는 하지 않았고 내가 이해할 수 있는 선에서 진행했다.

- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.
  - 1단계를 하기 전 https://github.com/cho-log/spring-learning-test 의 spring-mvc-1과 spring-mvc-2를 통해 학습했다. 이곳에서 학습한 것과 크게 다르지 않아서 본 미션 자체는 어렵지 않았던 것 같다.
  - 대신 learning-test 레포지토리에서 학습할 때는, 처음 접하는 개념과 용어가 너무 많았다보니, AI에게 질문을 많이 할 수밖에 없었다. 이 과정에서 AI에게 정답을 알려달라고 하지 않았다. 내가 답을 도출할 수 있게 활용할 개념을 알려달라고 하였다.
  - learning-test에서 학습을 마친 후, 내 언어로 학습한 내용을 정리했다. 3일 뒤에 이 정리한 문서를 내가 스스로 다시 작성해내야 한다.
    - 학습한 내용은 아래 따로 첨부하겠습니다.


### 3. 전략 평가
- 효과적이었던 것과 그 이유
  - 비교적 새로운 개념을 빠른 시간안에 이해하고 응용할 수 있게 되는 것 같다.
- 비효과적이었던 것과 그 이유
  - 3일 뒤에도 이 학습 내용을 잘 간직할 수 있을지 미지수다. 3일 후 스스로 문서를 작성하며 판단해야겠다. 만약 문서가 스스로 잘 작성되지 않았고, 작성했던 코드가 이해되지 않는다면 문서를 업그레이드하며 한번 더 작성한다. 그리고 3일 후 또 문서를 스스로 작성해보고자 한다.

- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?
  - 개념은 알고 있었는데 Spring 에서의 표현 방법과 내포된 의미를 모르는 것이 많았다. 따라서 AI에게 내가 원하는 구현과 설계를 설명하고 검토받은 후, 그것을 Spring 에서는 어떻게 표현하는지 배울 수 있었다.


### 4. AI 피드백
- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고,
  유용했던 제안 1가지 이상 기록

- ```
  유용한 제안 1 — 3일 뒤 Blank Page Recall의 기준을 미리 정해두세요
  지금 로그에 "3일 뒤 스스로 문서를 작성해보겠다" 고 했는데, 기준이 없으면 회고할 때 "그냥 대충 썼는데 됐나?"가 됩니다.
  지금 당장 이 질문을 로그 하단에 적어두세요:
  
  
  흐름
  
  Spring MVC에서 HTTP 요청이 들어왔을 때, Controller까지 도달하는 과정을 설명해봐.
  
  계층 설계
  
  1단계에서 Service 계층을 만들지 않은 이유는?
  
  어노테이션
  
  @Controller와 @RestController의 차이는? @ResponseBody가 없으면 스프링은 반환값을 어떻게 해석해?
  
  ResponseEntity
  
  ResponseEntity.ok(body) / ResponseEntity.ok().build() / ResponseEntity.created(uri).build() — 각각 언제 써?
  uri는 어디에 들어가고, 왜 필요해?
  
  AtomicLong
  
  AtomicLong을 쓴 이유가 뭐야? 그냥 long 쓰면 안 돼?
  
  
  3일 뒤, 이 질문에 답을 못 쓰는 부분 = 아직 내 것이 아닌 부분.
  ```

- ```
  유용한 제안 2 — "개념은 알지만 Spring 표현을 모른다"는 따로 기록할 가치가 있어요
  
  이번 막힘의 종류가 흥미롭습니다. 개념(MVC 흐름)은 알고 있었지만 Spring의 어노테이션/표현으로 매핑이 안 됐던 것인데, 이건 번역 문제예요. 내 설계 언어 → Spring 언어로 번역하는 능력.
  
  이 패턴이 레벨2에서 계속 반복될 거예요. 막힐 때마다 "개념 모름"인지 "번역 모름"인지 구분해서 기록해두면, 나중에 내 약점 지도가 만들어집니다.
  ```

### 5. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
  - 학습 사이클 자체는 유지하려고 합니다. 1단계의 학습은 지금 당장엔 꽤 만족하고 있기 때문입니다.
- 바꿀 것과 그 이유
  - 6번 3일뒤 스스로 답할 질문 섹션이 추가되어야 할 것 같다. 이 섹션은 지금부터 바꾸고자 한다.
  - 막힘의 종류에 "개념 모름"과 "번역 모름"을 구분해 기록하려고 합니다. 제가 Java 자체가 익숙하지 않고 Spring은 더더욱 익숙하지 않아서, 이런 구분도 추후 리뷰에 도움이 될 것 같다고 생각합니다.

### 6. 스스로 답할 질문 (3일 뒤, 5/1)

**흐름**

1. Spring MVC에서 HTTP 요청이 들어왔을 때, Controller까지 도달하는 과정을 설명해봐.

**계층 설계**

1. 1단계에서 Service 계층을 만들지 않은 이유는?

**어노테이션**

1. `@Controller`와 `@RestController`의 차이는? `@ResponseBody`가 없으면 스프링은 반환값을 어떻게 해석해?

**ResponseEntity**

1. `ResponseEntity.ok(body)` / `ResponseEntity.ok().build()` / `ResponseEntity.created(uri).build()` — 각각 언제 써?
2. `uri`는 어디에 들어가고, 왜 필요해?

**AtomicLong**

1. AtomicLong을 쓴 이유가 뭐야? 그냥 `long` 쓰면 안 돼?