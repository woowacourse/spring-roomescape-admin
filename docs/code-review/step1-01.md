# 🛠️ 기능/구현

### 📝 Feedback 01

    Dao와 Repository의 차이가 없습니다.
    구현체에서는 단순 위임할 뿐으로, 인터페이스화 한 실익이 없다고 생각하는데요.
    우선 Dao 인터페이스 들을 Repository의 구현체로 리팩토링 하시라는 걸로 리뷰를 드리고요.
    
    어떤 기준으로 인터페이스화를 선택하는지도 한번 남겨주세요

### 💬 Apply 01

#### Repository / DAO 분리의 의도

[이전 커밋](https://github.com/nn98/spring-roomescape-admin/commit/99753c7aaf55043aada0b7e340eb6016708aaf49) 에선 DAO 로직을
Repository 내부에 구현했었습니다!   
레포지토리 구현체에서 바로 작성하는 경우도 많음을 [이전 미션](https://github.com/woowacourse/java-janggi/pull/316#discussion_r3055110229) 에서도 로빈이
언급해 주셨었지만  
`실무에 적응`한다는 최종 목적을 위해선  
`정석에 대해 명확히 이해`한다는 과정이 필요하다 생각했어요.

도메인 Reservation 에 대응할 Repository 를,  
데이터베이스 reservation, reservation_time 각각에 대응할 Dao 를  
직접 구현해 보며 이후 이 로직이 어떻게 추상화될 것인지, 그 내부를 추론할 수 있도록  
이전 미션에서 학습했던 내용을 반영하려는 의도로 분리를 선택했습니다.

#### 인터페이스화

단순한 요구사항이기에 직접적인 구현으로 충분한 상황이었지만  
필요한 최소한의 구현보단 인터페이스로 의존을 추상화해놓는 것의 이점이 훨씬 크다 생각했어요.

크루들과 함께 북스터디를 진행하며 (조영호 개발자의 오브젝트)  
인터페이스를 도입해 내부를 추상화함으로써 얻을 수 있는 의존성 감소 효과가  
확장성 측면에서 매우 효과적이라 생각했고, 실증적으로 검증하기 위해  
이번 미션에서 직접 사전 분리와 기능 확장을 경험하려는 의도로 인터페이스화 해 보았어요.

#### 정리

과도한 분리와 근거/기준 없는 인터페이스화는  
개발 소요 증가와 클래스 과도화라는 역효과를 유발하기에 지양해야 마땅하겠지만

- 기능이 확장될 것으로 예상되는 미션(도메인)
- 이전 미션의 연장선으로 배운 내용을 실습
- 그 과정에서의 개념 학습과 기준 확립

을 목적으로 시도했습니다.

웨지의 피드백 덕분에 다시 한 번 생각을 되짚으며 구현의 근거를 찾을 수 있었네요.  
다만 제가 선제적으로 코드나 구현의 근거를 전달했다면 더 효과적으로, 더 효율적으로  
진행 가능했을 부분이라 스스로 반성하게 되었습니다.

학습은 분명 중요하지만 그 핵심 과정으로 리뷰/피드백이 존재하는 이상,  
효율적인 리뷰를 위해 더 많은 시간을 배분하고 일정을 조율해야겠다 생각했습니다.

### 📝 Feedback 02

> 인터페이스에 `@Repository`를 붙이는 게 의미가 있을까요? 해당 어노테이션이 왜 있는건지 학습해보셔요

### 💬 Apply 02

#### 인터페이스에 붙은 어노테이션

명백한 검증 누락입니다. 이전 미션에서 피드백 받은 내용을 반영하지 못했네요.

> 커밋 전 diff 확인과 자기 작업의 실제 결과를 검증하는 습관이 보강되면 완성도가 크게 올라갈 것입니다.

작업 단위를 잘게 쪼개고 커밋 전에 확인할 수 있도록,  
또 그럴 여유를 충분히 가지기 위해  
더 세부적인 일정 계획을 수립할 수 있도록 개선하겠습니다. 짚어주셔서 감사합니다!

#### `@Repository` 어노테이션

우선 직관적으로 와닿은 Spring Repository 의 차이점은 `throws SQLException` 의 배제였습니다.  
이전 미션에선 Repository / DAO 의 모든 Connection 메서드에 throws 구문을 붙여야 했지만  
그것이 생략되었다는 것은 Spring 이 어떤 방식으로나마 예외를 처리(not Solve)해 주기 때문이라 생각했어요.

어노테이션의 Javadoc 내용을 활용해 학습해봤어요.

`번역`

- 이 어노테이션이 붙은 클래스는 도메인 주도 설계(DDD)에서 정의한  
  "저장, 조회, 검색 행위를 캡슐화하여 마치 객체 컬렉션처럼 동작하는 메커니즘"인 'Repository' 임을 나타냅니다.
- [X] 전통적인 J2EE 패턴인 'DAO(Data Access Object)'를 구현하는 팀들도  
  이 어노테이션을 사용할 수 있지만, 적용하기 전에  
  DAO와 DDD 스타일 Repository의 차이를 이해하는 것이 좋습니다.
- 이 어노테이션이 붙은 클래스는 PersistenceExceptionTranslationPostProcessor와 함께 사용될 때,  
  하부 데이터베이스의 원시 예외를 스프링의 범용적인 DataAccessException으로  
  자동 변환(Translation)해 주는 자격을 얻습니다.
- 또한, 이 어노테이션은 @Component의 특수한 형태이므로 클래스패스 스캔을 통해  
  스프링 컨테이너에 자동으로 빈(Bean)으로 등록됩니다.

생각했던 대로 `데이터베이스의 원시 예외를 범용직인 예외로 변환` 한다는 내용이 포함되어 있었습니다.  
또한,  
데이터베이스의 원시 예외는 사용하는 기술에 따라 다르기에 그것을 직접 던진다면  
서비스는 예외 처리에 데이터베이스의 종류에 의존하게 되고, 그것을 방지하기 위해  
`@Repository` 어노테이션이 예외를 변환해서 던져 주는 것임을 확인할 수 있었어요.

이런 계층간 의존/종속 제거에 [앞선 답변]() 내용이 포함된다 생각했어요.  
예외 처리 측면에서 예외를 포장해서 던짐으로써 하부 데이터베이스의 종류에 대한 종속을 제거하듯  
기능 구현 측면에서 저장소를 인터페이스로 추상화함으로써 동일한 종속 제거가 가능한 것이 아닐까요?

#### 정리

일단 여전히 셀프 검증과 디테일 체크가 부족함을 확인할 수 있었어요.  
처음에 클래스를 생성하며 자연스럽게 어노테이션을 붙이고  
인터페이스로 변경 -> 구현체 구현 과정에서 내내 까먹고 있었네요..

diff 나 IDE 기능을 활용해서 검증하는 습관을 들여보도록 하겠습니다. 감사합니다!!

실제 구현 과정에서 북스터디나 학습에서 배운 내용이 부분부분 튀어나오니  
소소하게 보람도 있고 바로바로 나름의 답변을 낼 수 있다는 것이 매우 즐겁습니다.  
틀린 부분이나 더 공부할 부분 팍팍 지적해주시면 열심히 고치고 배우겠습니다!

### 📝 Feedback 03

    Presentation Dto가 레포지토리의 argument까지 침습했는데요
    
    Request를 왜 만드셨나요? 레이어 간 구분짓기 위함이 아니었나요? 레이어를 너무 깊게 들어온 것 같네요.
    (아마 ID처리 때문일거라고는 예상합니다만)
    
    여기선 도메인 객체나 repository 레이어 DTO를 넘길수 있게 리팩토링 해보시고 결과를 알려주세요

### 💬 Apply 03

#### DTO 를 활용한 계층 간 의존 제거

도메인 객체 저장 / 생성을 위한 정보를 모두 포함하고 있어 재사용했지만  
Request 객체를 만든 근본 목적인 레이어 간 구분을 파괴하는 재사용이었네요.

같은 정보를 전달하더라도 다른 계층 간 전송엔 다른 DTO 를 활용해야  
변경의 전파를 줄이고 의존을 끊어낼 수 있음을 숙지하겠습니다.  
DTO 를 패키지로 묶어 관리하기보다 각 계층에 두는 것도 import 문을 확인해서  
계층 간의 일방향 의존이 깨졌는지 확인할 수 있는 좋은 방법이겠네요!

#### 도메인 객체의 무결성에 대한 실무적 타협

다만 정말로 모든 계층 간 DTO 를 분리하면  
서비스가 복잡해질수록 클래스 폭발의 위험이 심각하기에  
어떤 해결 방법이 있을까 찾아봤어요.

도메인은 그 자체로서 무결성과 완결성을 보장해야 하지만  
식별자 생성 전, 엔티티가 되기 전 VO 상태인 도메인 객체를 표현하기 위해  
비영속(transient) 상태의 도메인 객체를 허용하는 방식을 택했어요.

```java
public record Reservation(long id, String name, LocalDate date, long reservationTimeId) {

    public static Reservation transientOf(ReservationRequest reservationRequest) {
        return new Reservation(0, reservationRequest.name(), reservationRequest.date(), reservationRequest.timeId());
    }
}
```

비영속 상태로 전달되어 저장(영속화)되고, 이후 새로운 객체를 조회해서 반환한다면  
불완전한 도메인이지만 요청 - 전달 - 저장 이라는  
짧은 라이프사이클에 한정되도록 할 수 있다는 생각이었어요.

#### 정리

객체 지향적 프로그래밍을 위해, 계층 간 의존을 제거하기 위해 도입된 DTO 는  
무작정 지양하지도, 마구잡이로 도입하지도 말아야 하며  
그 근본적인 목적과 목표를 준수할 수 있도록 고민하고 활용해야 함을 상기할 수 있었습니다.

피드백 04 와도 연관된 내용이지만  
도메인과 데이터베이스의 필연적 차이가 꽤나 다양한 문제와 고민을 유발함을 알 수 있었어요.  
또 transient 용어에 대한 탐색 과정에서 별 생각 없이 사용했던 다양한 도구가  
선배 개발자들의 수많은 고민의 결과였음을 재확인하는 경험이었어요.

### 📝 Feedback 04

> id가 아니라 객체 자체를 반환하게 해보세요.  
> 지금 메서드에서 create + select 두 번의 쿼리가 나가고 있습니다.  
> 1번의 쿼리로 처리할 수 있는 구간인데 return 타입의 문제네요.

### 💬 Apply 04

> `도메인 객체` 라 명시하지 않으신 점이 힌트이지 않을까 생각했어요.

### 쿼리의 분리

먼저, 미션의 API 명세를 참고하면 생성과 함께 생성 결과를 반환해줘야 하는데  
이 부분은 [학습 로그](../study-log/log-02.md)에서 다룬 CQS 에 대한 `실용적 예외` 로 일부 해결되는 부분이지만  
그 위치가 여기가 맞을지 고민해 보고 서비스로 해당 로직을 이관했어요.

### `객체`의 반환

그 다음, 어떤 객체를 리턴하고 응답할지 고민해 보았어요.  
[Apply 03](https://github.com/woowacourse/spring-roomescape-admin/pull/452#discussion_r3176528655) 에선 VO -> Entity 과정에
대한 고민으로 transient 객체를 사용했고,  
이번엔 도메인 객체 간의 포함 관계를 고민해 보았습니다.

- 🎯 `도메인 객체`

이전 미션에서의 학습을 기반으로, 도메인 객체를 3가지로 분류할 수 있다 생각했어요.

- 애그리거트 루트
    - 엔티티이자 포함하고 있는 하위 엔티티와 VO 에 대한 접근점, Repository 와 대응
- 엔티티
    - 고유한 식별자를 기반으로 자신만의 라이프사이클을 보유한 도메인 객체
- VO
    - 값과 책임을 보유하고 있지만, 상태만을 가지고 자신만의 식별자가 없는 도메인 객체

이미 구현된 구조를 배제하고 생각해 볼 때,  
Reservation 과 ReservationTime 각각이 무엇에 해당하는가?

- 아직 별도의 행위는 없지만 예약과 시간을 저장한다는 비즈니스 로직은 보유 O
    - VO 충족, 도메인 객체 충족
- 고유한 식별자 보유 O
    - 엔티티 충족
- `접근점` 인가?

일단 직관적으로 Reservation 은 모든 조건을 충족하기에 애그리거트 루트라 생각했어요.

- ReservationTime 은 애그리거트 루트인가?
    - Reservation 내부에 ReservationTime, 정확히는 ID 가 있는데?
- 단순히 내부 필드로 존재한다고 애그리거트 루트가 아니라 단정할 수 있는가?
    - 별도의 엔드포인트도 존재하고, 그에 따라 DB 에 접근할 저장소까지 구현되어 있는데?

그 외에도 여러 고민을 했지만 엔드포인트가 존재하고 그에 따른 Repository 가 존재한다는 점이  
애그리거트 루트라 판단하는 결정적 근거가 되었어요.

- 🎯 `애그리거트 루트` 간의 참조

도메인 객체 간의 의존/협력이 아닌  
애그리거트 루트 간의 참조를 객체 내부 필드로 구현해야 할 지,  
객체 간 참조와 의존으로 구현하되 필요에 따라 필드로 표현해야 할 지 고민했으나

결국 어떻게든 도메인 객체 간의 의존을 최소화하는 것이 바람직하다 판단하고  
식별자 기반 참조로 구현하기로 결정하고,  
기존 Reservation 객체에서 ReservationTime 필드를 long reservationTimeId 로 변경했습니다.

- 🎯 `조회 전용 객체`

위와 같이 변경한다면 API 명세에서 요구한  
`GET /reservations` `POST /reservations` 의 응답은 어떻게 표현할 것인가?

다행스럽게도 [학습 로그](../study-log/log-03.md)를 정리하는 과정에서  
DB 단에서 join 을 통해 모든 데이터를 조회해 오도록 구현했기에  
응답을 위한 모든 데이터를 조회할 수 있었지만

식변자 참조 방식으로 변경했기에 도메인 객체를 그대로 반환할 수 없다는  
충돌 상황을 마주하게 되었습니다.

이에 응답 구조에 맞춰 모든 정보를 담은 조회 전용 객체, ReservationJoinedDto 를 생성하고  
Repository 에서 반환하도록 수정했습니다.

도메인 객체와 저장소의 1:1 대응을 준수하면서 응답 구조를 생성하기 위해  
두 번의 조회를 실행하고 응답을 조립하기보단  
SRP, 이론적 DDD 준수를 일부 포기하더라도  
단일 쿼리로 응답을 생성하는 것이 훨씬 바람직하기 때문입니다.

#### 정리

결국 `객체`라는 키워드 하나로 열심히 머리를 굴려가며 개념에 대한 보강과  
나름의 논리 전개를 시도해 보았는데 웨지의 의도를 잘 파악한 것일지,  
그렇지 않더라도 올바른 방향성으로 생각하기라도 한 것일지 걱정되네요.

이런 작은 크기의 서비스에서도 수많은 고민과 구현, 변경과 확장이 반복되는데  
실무에서의 복잡다원한 서비스를 관리하기 위해선  
얼마나 많은 이론적 기반과 배경 학습, 자신만의 기준과 트레이드오프가 필요할지  
두려움 반 기대심 반의 마음입니다..

### 📝 Feedback 05

    도메인 객체에선 date의 적절한 자료형으로 선언해주세요.
    
    그리고 도메인 객체인데 아무런 validation이 없어요.
    학습은 복리로 계속 쌓여나가야 합니다. level1 때 배운것은 level2에도, level2에 배운 것은 level3에도 다 적용되어야 합니다. 당시 했던 도메인에 대한 깊은 고민을 이번 미션에도 "당연하게" 적용해주세요

### 💬 Apply 05

#### 자료형 변경

예제에 얽매이기보다 더 바람직한 방향으로 개선하는 것이 중요하겠네요.  
예제와 요구사항도 결국 스스로의 기준 수립과 학습을 위한 가이드란 점 인지하고  
더 올바른 방향으로 학습하고 구현할 수 있도록 하겠습니다.

#### 학습 내용의 반영과 점진적 발전

도메인 객체에 별다른 행위가 없다고 너무 해이해졌던것 같습니다.  
엔티티의 식별자 유무에 따른 무결성 걱정에 앞서  
기본적인 상태에 대한 검증이 선결되었어야 하는데 까맣게 잊고있었네요.  
도메인 객체의 유효성 검증을 추가하고  
자료형 변경을 통한 기본 검증(LocalDate/LocalTime)을 활용했습니다.

#### 정리

항상 기본을 기억하기

### 📝 Feedback 06

> 단위 테스트가 없네요. 다음 리뷰 때 단위테스트 구현해서 요청주셔요.

### 💬 Apply 06

### 📝 Feedback 07

> 시간에 적합한 DB 자료형을 적용해보세요

### 💬 Apply 07

[Feedback 05](https://github.com/woowacourse/spring-roomescape-admin/pull/452#discussion_r3176690056) 에서 일괄적으로 Date/Time
자료형을 변경했습니다!

### 📝 Feedback 08

> 꼭 필요한 객체일까 싶네요. ReservationTimeResponse의 메서드로 있어도 될거같은데요.

### 💬 Apply 08

#### Util 클래스의 필요성

초기 구현에선 ReservationResponse 를 생성하기 위해  
ReservationTime 의 정보가 필요했고,  
이 정보를 포장하기 위해 ReservationTimeResponse 를 생성하다 보니  
중복된 호출이 발생해 유틸 클래스로 묶어서 관리하려는 의도였습니다.

매핑이 중복되지 않는 Reservation 은 매퍼로 분리하지 않긴 했으나  
확실히 일방향 매핑, 단일 메서드, 적은 중복이라는 상황을 고려해 보면  
불필요한 분리와 클래스 생성이었던 것 같습니다.

#### 정리

정적 팩토리 메서드를  
적절 하게 사용하고

> 매핑 코드를 자동으로 만드는 것보다 더 좋은 것은,  
> 아예 매핑할 필요가 없는 구조를 만드는 것이다.

격언을 잊지 말기

---

# 📚 학습법

### 📝 Feedback 09

    ADR은 잘 모르겠습니다.
    
    ADR은 큰 프로덕션 단위의 레포지토리에서 유지보수의 용이함을 위해 설계된 디자인입니다.
    특정 레포지토리에 한정되어있는 점에서 개인 학습 용도에는 들어맞는 모델은 아니라고 생각해요.
    
    다만 핵심 개념만 따와서 본인의 기술 의사결정을 잘 기록해두는 건 가치가 높다고 생각합니다.

### 💬 Apply 09

확실히 기법 v3의 하단은 너무 과도하거나 오버한 학습법이라 생각해서  
부분적으로 응용해보거나 인지하는 정도로 활용할 생각이었어요.

다만 크루들과 토론하며 생각해낸 저만의 효율적 기록 방식이

> 개념이나 내용을 정리하기보단 학습 과정에서 내가 생각하고 결정했던  
> `학습의 흐름` 을 기록해 자연스럽게 그 과정을 체화하고  
> 나중에 다시 살펴보더라도 그 흐름을 기억하며 떠올릴 수 있도록 하기

였다 보니 기술적인 의사결정 과정이나 그 근거, 생각의 흐름을 남기는 것은  
이와 일맥상통한다 생각해서 적극 반영해보고 있습니다!

### 📝 Feedback 10

    가끔 한 번 해볼만 하지만 학습 시스템화 하기엔 현실성이 낮아요.
    
    임상시험의 개념 중 Efficacy(효능)과 Effectiveness(유효성)의 차이성을 설명하는 게 있는데요.
    
    효능은 통제환경에서 인과관계가 증명된 것,
    유효성은 현실세계에서 해당 효능이 잘 동작할지
    구분하는 겁니다.
    
    예를들어 올리브유를 매달 2병씩 까마시면 10년 더 장수할 수 있다고 할때, 올리브유를 모든 가정에 매달 2병씩 지급하는 정책을 펴겠다고 설계해보겠습니다.
    이 정책은 효능이 입증되었으나 유효성은 낮을 수 있습니다. 실제로 올리브유를 2병씩 퍼먹으라는 게 현실세계에선 잘 동작하지 않으니까요. (여러 이유로 안 먹겠죠)
    
    이 방법론도 비슷하게 효능은 있으나 유효성이 떨어지는 방식이라고 생각합니다.
    일단 안티패턴이라는 걸 수집하는 게 쉽지 않습니다. 안티패턴을 어떻게 수집할건가요? 인터넷에 쳐서 안티패턴이라고 나온것들을 모아볼까요?
    그리고 150가지의 안티패턴이 정의되었으면, 프로덕션 10줄을 위해 150개의 방법을 수행해볼 수 있으려나요?
    
    가끔 한번씩 써볼만한 tool 정도로 내장해두고, 좀더 대부분의 상황에 맞는 보편적인 학습법을 하나 구상해보시면 좋겠네요`

### 💬 Apply 10

아주 직관적이고 누구라도 이해하고 납득할 수 있을 예시였던 것 같아요. 😄  
10년의 평균수명 연장에 소모되는 자원보다 올리브유 배급이 훨씬 합리적/효율적이지만  
그 현실성/유효성은 결코 정비례하지 않을 테니까요.

다행스럽게도 레벨1 선택미션이었던 `당연한 원칙, 직접 검증하기` 가  
그 실증적 증거가 되어줄 것 같네요!  
당연하게 생각했던 TDA 원칙을 의도적으로 위배하고  
위배했을 때 / 준수했을 때 각각의 코드에 대한 유지보수 소요를 비교하는 미션이었는데  
미션의 가설 수립 / 실험 설계 / 결과 검증 그 자체에서 어마어마한 고민과 노력이 필요했고  
그 결과도 깨달음은 주었을지언정 유효성은 아쉬웠었어요.

분명 필요했고 보람찬 경험이었다고 생각하지만,  
기본적으로 상시 견지할 만한 학습법은 결코 아닐 것 같아요 😂🤣

### 📝 Feedback 11

> 매우 좋은 지침이네요.

### 💬 Apply 11

저도 실제 적용은 아직이지만 효과적인 방법이라 생각했어요!

치밀하고 꼼꼼한 설계가 바람직한 코드 구현의 기반이 되는 것은 분명하지만  
그렇다고 설계에 집작하다 구현이 미뤄지는 것은 최악의 문제상황이라  
적절한 기준점을 설정하는 것에 고민이 많았는데

이런 식으로 동작 확인을 위한 스파이크 작성과 실제 구현을 투트랙으로 시도한다면  
구현과 동작을 확인했으니 실제 설계나 최종 구현에 꽤나 효과적일 것 같아요.

특히나 페어 프로그래밍에서 머리속으로 떠오른 내용만을 가지고 페어를 설득하기 쉽지 않은데  
이미 구현해 본 기능이라면 충분히 그 근거나 기준점이 되어 주지 않을까 싶네요!

### 📝 Feedback 12

> 마지막 2줄에 크게 공감이 됩니다.

### 💬 Apply 12

그 공감이 제게 큰 위안과 응원이 되네요 😂  
한 번에 완벽히 해내긴 어려울지라도 항상 근본을 잊지 않도록 노력하겠습니다!  
