### 단계 전환 체크
- 이전 단계에서 쓴 접근법을 그대로 쓸 것인가, 바꿔볼 것인가?
  - 그대로 쓸것이다.
- 바꾼다면 왜? 바꾸지 않는다면 왜?
  - 시간 API 추가와 예약-시간 연결도 결국 처음 보는 개념을 작은 단위로 나눠 구현하는 방식이 더 효율적이라고 느꼈다.
- 이전 단계에서 발견한 "나에게 맞는 전략"이 새 단계에서도 통할 것 같은가?
  - 테이블 추가, 외래키, join처럼 새로운 요소가 있었지만 기본 학습 방식은 그대로 통할 것이라고 생각했다.


## 학습 로그 #03

**시간** : 04/30 12:00 ~ 13:10 (약 70분)

**학습 범위** : 3단계 시간 관리

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)
- [x] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [x] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략
- 시간 관리 기능과 예약-시간 연결을 분리해서 봤다.
- `reservation_time` 테이블 추가, `timeId` 요청값 변경, `inner join` 조회처럼 지금 필요한 것부터 순서대로 확인했다.
- 기존 2단계 코드를 기준으로, 바뀌는 부분만 찾아서 도메인, DAO, 서비스, API를 연결했다.

### 3. 전략 평가
- 효과적이었던 점은 변경 범위를 한 번에 보지 않고 테이블, API, 객체 관계로 나눠서 본 것이다.
- 시간 API를 먼저 만들고 그 다음 예약과 연결하니 전체 흐름을 이해하기 쉬웠다.
- 이번 막힘도 개념이 낯설고 코드 사용법이 익숙하지 않은 문제였기 때문에, 예제 쿼리와 테스트 시나리오를 바로 보며 구현하는 방식이 잘 맞았다.

### 4. AI 피드백
- 외래키와 join이 들어가는 단계일수록, 스키마 변경과 요청/응답 형식 변경을 먼저 고정한 뒤 코드로 내려오라는 피드백이 유용했다.

### 5. 다음 타임에 바꿀 것
- 유지할 것:
    1. 문제에 직접 필요한 개념부터 확인하고 바로 코드에 적용하는 흐름.
    2. 기존 단계 코드에서 달라지는 부분만 집중해서 보는 방식.
    3. 낯선 기술은 예제 쿼리와 테스트 시나리오를 먼저 보고 구현하는 방식.
- 바꿀 것:
    1. 도메인 관계가 추가되는 단계에서는 요청/응답 구조와 테이블 구조를 먼저 함께 정리한 뒤 구현하겠다.
