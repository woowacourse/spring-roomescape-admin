## 학습 로그 #4

**시간**: 04/29 01:30 ~ 03:00 (약 1시간 30분 학습)
**학습 범위**: (4단계 계층 분리)

### 1. 막힌 것의 종류

이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)

- [ ] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [ ] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [ ] 기타:

### 2. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
    - > 전략: 구조가 바뀌면 빨간 줄부터 고치지 말고, 입력·저장·조회 등 흐름 및 변경할 점을 먼저 생각해본 뒤 수정한다.
    - 실행 🟢
        - 이유: service, dto 가 도입되기 전에 미리 각 계층의 책임을 생각하며, 어떤 코드들을 변경시킬 지 떠올렸다.
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.
    1. service, dto의 책임에 대해 학습한다.
    2. 빨간 줄이 뜨는 곳들을 수정한다.
    3. 제공된 테스트 및 postman을 통해 코드가 예상한 대로 동작하는 지 확인한다.

### 3. 전략 평가

- 효과적이었던 것과 그 이유:
    - 구조 변경 전 미리 능동적으로 변경할 코드에 대해 생각한 것이 효과적이었다.
    - 계층 분리라 코드 수정 범위가 컸기 떄문에, 리팩토링 후 테스트가 깨질 가능성이 높았다.
    - 하지만 미리 각 계층의 책임 및 변경할 부분에 대해 생각해두었기에, 빨간 줄이 많이 떴지만 문제없이 수정할 수 있었다.
- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?:
    - 변경 범위에 대해 미리 생각해두었기에, 생각해둔 부분들을 놓치지 않고 수정할 수 있어서 좋았다.

### 4. AI 피드백

- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고,
  유용했던 제안 1가지 이상 기록

```text
Compare: 예상과 실제 차이를 비교한다.
실제 변경 범위가 예측한 변경 범위와 얼마나 일치하는지 비교하는 것이다.
구조 변경 시 어떤 코드가 함께 영향을 받는지 예측하는 능력을 키울 수 있다.
```

### 5. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
    ```
  구조가 바뀌면 빨간 줄부터 고치지 말고, 입력·저장·조회 등 흐름 및 변경할 점을 먼저 생각해본 뒤 수정한다.
  
  능동적으로 사고한 후, 코드를 수정하기 때문에
  에러 발생 확률이 더 적을 것 같다. 
    ```

- 바꿀 것과 그 이유
