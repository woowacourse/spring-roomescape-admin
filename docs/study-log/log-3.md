## 학습 로그 #3

**시간**: 4/28 11:00 ~ 12:00 (약 60분)
**학습 범위**: 4단계 계층 분리

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)
- [ ] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [ ] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [x] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
```aiignore
바꾸기로 한 것은 없으며 종료 조건을 추가하는 것으로 진행하였습니다.
핵심 요약에서 종료 조건을 추가하여 너무 깊은 학습으로 가는 것을 방지했습니다. 
```
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.
```aiignore
이번 학습에서는 Spring의 Layered Architecture를 다시 정리했습니다.
이전에도 한 번 학습했던 내용이지만 시간이 지나면서 개념이 휘발되어 실제 코드 작성 과정에서 계층의 책임 경계가 애매해지는 지점이 발생했고,
이를 다시 정리할 필요가 있었습니다.

특히 레벨 1에서 객체지향 설계를 비교적 엄격하게 적용하면서 객체의 응집도와 결합도를 기준으로 설계를 진행하는 습관이 자리 잡혀 있었는데,
이 관점이 Spring의 레이어드 구조와 함께 적용되면서 설계를 어떻게 가져가야 할지에 대한 고민이 생겼습니다.
객체 내부에 책임을 두는 방식과 계층 단위로 책임을 분리하는 방식이 동시에 고려되면서,
어떤 기준으로 설계 방향을 결정해야 하는지가 중요한 판단 포인트가 되었습니다.
이에 대해 단순히 하나의 방식이 정답이라고 보기보다는, 전체 흐름은 레이어드 아키텍처를 기준으로 안정적으로 분리하고,
변하지 않는 도메인 규칙은 객체지향 원칙을 적용하여 객체 내부에 캡슐화하는 방식으로 설계하는 것이 적절하다고 정리했습니다.
즉, 구조적인 책임 분리는 계층에서 담당하고, 도메인의 핵심 규칙과 상태 보호는 객체가 책임지는 방식으로 균형을 맞추는 것이 현실적인 접근이라고 결론 내렸습니다.
```

### 3. 전략 평가
- 효과적이었던 것과 그 이유
```aiignore
흐름 중심으로 구조를 이해한 방식이 효과적이었습니다.
단순히 개념을 외우는 것이 아니라 요청 → 서비스 → 저장 → 응답 흐름으로 보니
각 계층의 역할이 자연스럽게 구분되었습니다.
```
- 비효과적이었던 것과 그 이유
```aiignore
“이 구조가 맞는지”에 대한 판단 기준이 아직 명확하지 않아
코드가 맞는지 스스로 확신을 내리기 어려웠습니다.
특히 DTO 분리 여부나 Entity 사용 범위에서 기준이 부족했습니다.
```
- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?
```aiignore
“코드는 돌아가지만 구조가 맞는지 모르겠다” 유형에는
단순 개념 학습보다 “설계 기준(why)” 중심 학습 전략이 더 적합했습니다.

현재 전략은 흐름 이해에는 적합했지만,
설계 판단 기준까지는 충분히 포함하지 못한 상태입니다.
```

### 4. AI 피드백
- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고,
  유용했던 제안 1가지 이상 기록
```aiignore
이미 이해 -> 정리 -> 구현 순으로 잘하시고 계십니다.
그러나 정보를 이해하는 것과 꺼내서 재구성하는 것은 다릅니다.

즉, 이해한 정보를 꺼내서 재구성하는 것으로 '아는 것 같은데 막히는 상황'을 막고 장기 기억 전환 속도를 증가시켜야합니다.
```
### 5. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
```aiignore
지금처럼 “무엇을 배우는가”보다 “왜 지금 필요한가”를 먼저 정의하는 방식은 유지할 생각입니다.
이 단계가 있어야 학습 대상이 추상적인 개념이 아니라 실제 해결해야 할 문제로 전환되기 때문입니다.
결과적으로 학습 범위가 자연스럽게 줄어들고, 불필요한 깊이 탐색을 방지할 수 있습니다.
```
- 바꿀 것과 그 이유
```aiignore
기존에는 핵심 압축 이후 바로 코드로 들어갔지만, 이제는 그 사이에 자료 없이 개념을 다시 재구성하는 단계를 추가할 것입니다.

이유는 단순합니다. 지금까지의 구조는 “이해한 것처럼 보이는 상태”를 걸러내지 못하는 문제가 있습니다.
즉, 외부 자료 기반으로 정리된 내용이 실제로 내 머릿속에서 재구성 가능한지 확인되지 않았기 때문에 다시 재구성하는 단계를 추가하는 것입니다.
```
