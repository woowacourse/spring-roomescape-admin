spring:
  h2:
    console:
      enabled: true  # H2 콘솔 활성화
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:database  # 인메모리 H2 DB 사용

  thymeleaf:
    cache: false
    prefix: classpath:/templates/admin/  # 템플릿 경로
