## 학습 로그 #2

**시간**: 04/29 17:00 ~ 17:50, 19:00~ 20:00 (약 110분)
**학습 범위**: 2단계 DB 연동

### 1. 막힌 것의 종류

이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)

- [ ] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [ ] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [ ] 기타: 코드작성 방법도 모르고, 개념도 모른다.

### 2. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
  나선형 학습을 위해 돌아오는 것을 강제한다.

- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

2단계 미션은 JdbcTemplate을 이용한 DB 적용 입니다.
JdbcTemplate 사용법을 몰라 AI를 이용해서 사용법을 학습하려 했습니다.

학습을 위해 사용한 프롬프트는 다음과 같습니다.
"JdbcTemplate 공식 문서 기반으로 나의 학습을 도와줘. 정답을 절대 알려주지 말고, 문법사용과 원리를 이해하도록 질문을 해줘"

AI와 질의 응답을 하면서 Spring에서 connection, transaction과 같은 흐름을 통제해주기 때문에 JdbcTemplate에서는 데이터만 신경쓰면 된다는것을 알게 되었습니다.
하지만 코드 작성은 다른 문제였습니다. 공식문서 기반으로 코드를 작성하기는 까다로웠고, 결국 블로그를 찾아보며 코드를 작성했습니다.
그 과정에서 생긴 의문에 대해 정리해 보았습니다.

1. RowMapper를 왜 사용하는지
2. ResultSet을 바깥으로 빼면 안되는지
3. Connection을 어디서 관리하는지
4. Transcation이 걸렸을때 같은 커넥션을 어떻게 받아오는지
5. NamedParameterJdbcTemplate vs JdbcTemplate 차이가 뭔지

1,2번은 강하게 연결되어 있습니다.
ResultSet은 Connection에 강하게 연결되어 있기 때문에 close를 자동으로 해주는 JDBC 템플릿에서는 인라인으로 처리할 필요가 있습니다.
이때 사용하는 객체가 RowMapper이고 람다 형식으로 객체를 반환합니다.
Spring에서 흐름을 관리하고, 이때 ResultSet을 반환하면 안되기 때문에 RowMapper를 이용함을 알게 되었습니다.

3. DataSource에서 Connection을 관리하고 이때 커넥션 풀을 사용한다는 것을 알게 되었습니다.
   -> 다음 사이클 : 커넥션 풀 초기 개수, 개수가 어떻게 늘어나는지, 개수가 늘어났을때 유지가 되는지 or 일정시간이 지나면 사라지는지 확인

4. Transaction의 경우 TransactionManager에서 트랜잭션이 걸린 경우에 새로운 Connection이 아닌 트랜잭션과 동일한 커넥션을 Tread에 묶는 방식으로 전달한다는 것을 알았습니다.
   -> 다음 사이클 : TransactionManager 트랜잭션이 걸렸을때 어떻게 같은 커넥션을 반환하는지 탐구.

5. 공식 문서를 보다보니 NamedParameter 있는데, 뭔 차이인지를 모르겠어서 질문했습니다.

JdbcTemplate 의 경우

```java
jdbcTemplate.update("INSERT INTO Reservation (name, date, time) VALUES (?, ?, ?)",
                    reservation.getName(),reservation.

getDate(),reservation.

getTime());
```

NamedParameterJdbcTemplate의 경우

```java
String sql = "INSERT INTO reservation(name, date, time) VALUES (:name, :date, :time)";
Map<String, Object> params = new HashMap<>();
        params.

put("name",reservation.getName());
        params.

put("date",reservation.getDate());
        params.

put("time",reservation.getTime());

long update = jdbcTemplate.update(sql, params);
```

버그를 줄이기 위해 NamedParameter를 사용하기로 결정했습니다.

### 3. 전략 평가

- 효과적이었던 것과 그 이유
  코드 작성은 블로그를 이용, 개념이나 흐름을 학습하는데에는 공식문서 기반 AI 질의응답.

- 비효과적이었던 것과 그 이유
    1. 하다보니 AI가 정답을 알려주게 된다.

```
JdbcTemplate이 ResultSet을 외부에 노출하지 않는 이유는
( )과 ( )를 보장하기 위해서다

힌트:

자원 관리
안정성 / 생명주기
```

정답을 유도하는 질문을 한다.
이건 내가 정답에 접근하지 못하고 계속 트라이 하다보니 AI가 위처럼 힌트를 제공해주는 방식으로 정답을 유도했다.
베이스 지식이 없을때는 어쩔수 없다고 느껴진다. -> 나의 주도적인 학습이 필요하다.

- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?

### 4. AI 피드백

- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고,
  유용했던 제안 1가지 이상 기록

```
🔥 딱 하나의 개선 (핵심)
👉 “정답 도달 금지 구간”을 만들어라

❌ 지금 방식:

질문 + 힌트

⭕ 개선 방식:

“힌트 없이 내가 답을 시도할 시간을 줘”

---
구체적 적용

### Step 1

- AI 질문 받기

### Step 2

👉 3~5분 동안 무조건 혼자 답 시도

- 틀려도 상관 없음
- 말로 설명하거나 적기
### Step 3

그 다음에만 힌트 요청

### 핵심 차이

기존:
- 질문 → 힌트 → 답

개선:
- 질문 → 자기 시도 → 힌트 → 수정
```

### 5. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
    1. 나선형 구조에서 재방문하는것을 강제하기.
    2. 코드 작성은 블로그와 같이 스스로 찾아보며 학습.
    3. 흐름이나 개념은 AI를 이용한 질의응답.

- 바꿀 것과 그 이유
    1. 정답 도달 금지 구간 만들기 -> 틀려도 내 생각을 구조화할 시간이 필요하다. 그 이후 깨지고 붙으면 더 견고한 구조가 머리속에 저장될 것 같다.
    2. 사이클 구조화 - 글쓰기 미션을 하다보니, 퇴고를 하는 과정에서 매 사이클마다 다른것에 집중한다는 것을 알게되었습니다.
       첫번째 사이클에서는 글의 흐름에 집중하고
       두번쨰 사이클에서는 끊기는 부분이 없는지 집중합니다.
       마지막 사이클에서는 잘못 사용한 단어가 있는지, 오탈자가 있는지 체크한다고 합니다.
       저의 학습에서도 리뷰를 사이클 기준으로 하지 않고, 각 사이클마다 역할을 나눠보려고 합니다.

        1. 코드 작성에 집중한다. 이때 생긴 의문점을 기록해둔다. - AI 사용x
        2. 핵심 키워드를 기반으로 왜 그것을 사용하는지 목적을 학습한다.(ex JdbcTemplate, @Controller) - AI 사용o
        3. 코드작성하며 들었던 의문점을 AI와 토의하면서 2번 과정과 연결지어 학습한다.
           -> 깊게 들어가지 않고, 의문점을 해결했을때 나오는 키워드를 기록해둔다. 다음 사이클을 위한 학습 재료가 된다.

