### 단계 전환 체크
- 이전 단계에서 쓴 접근법을 그대로 쓸 것인가, 바꿔볼 것인가?
  - 그대로 쓸것이다.
- 바꾼다면 왜? 바꾸지 않는다면 왜?
  - 처음보는 개념인 것과 "코드로 어떻게 쓰는지 모르겠다"라는 문제에 대해서 빠르고 효율적인 학습법이라고 느꼈다.
- 이전 단계에서 발견한 "나에게 맞는 전략"이 새 단계에서도 통할 것 같은가?
  - 이전과 크게 다른 상황이 아니기 때문에 통할 것이라고 믿는다.


## 학습 로그 #02

**시간** : 04/29 15:00 ~ 14:52 (약 52분)

**학습 범위** : 2단계 DB 연동

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)
- [x] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [x] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략
- DB 연동에 필요한 개념을 먼저 작게 나눠서 봤다.
- `JdbcTemplate`, `schema.sql`, `application.properties` 설정처럼 지금 바로 필요한 것부터 확인했다.
- 예제 SQL과 테스트 코드를 같이 보면서, 메모리 저장을 DB 저장으로 바로 바꿔 적용했다.

### 3. 전략 평가
- 효과적이었던 점은 필요한 개념만 먼저 확인하고 바로 구현에 연결한 것이다.
- DB 연결, 테이블 생성, 조회/추가/삭제를 한 번에 보지 않고 나눠서 보니 덜 막혔다.
- 이번 막힘은 개념도 낯설고 코드 사용법도 익숙하지 않았기 때문에, 예제를 바로 보고 적용하는 방식이 잘 맞았다.

### 4. AI 피드백
- H2 설정, 스키마 생성, JdbcTemplate 사용처럼 지금 단계에 필요한 최소 흐름부터 잡고 구현하라는 피드백이 유용했다.

### 5. 다음 타임에 바꿀 것
- 유지할 것:
    1. 문제에 직접 필요한 개념부터 확인하고 바로 코드에 적용하는 흐름.
    2. 구현과 학습을 바로 연결하는 방식.
    3. 낯선 기술은 예제 코드를 먼저 보고 따라가며 이해하는 방식.
- 바꿀 것:
    1. 처음부터 전체 구조를 한 번에 이해하려 하기보다, 설정과 핵심 API 흐름부터 먼저 잡겠다.
