# 5+ 단계

## controller에서  try catch하여 적절한 응답 코드를 처리한다

### 대안

별도로 컨트롤러에서 발생하는 예외를 담은 handler을 활용한다.

### 이유

현재 규모에서는 커스텀 예외가 하나 뿐이라 try-catch 하나로 해도 코드가 복잡해지지 않음

### 틀렸을 때 증상

같은 예외 처리가 컨트롤러마다 반복된다.

