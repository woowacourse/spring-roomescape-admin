## 학습 로그 #3

**시간**: 04/28 21:30 ~ 23:40 (약 120분)
**학습 범위**: 3단계 시간 관리

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)
- [ ] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [ ] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [x] 기타: 단순 실수

**내용**:  
`schema.sql`에서 `reservation` 테이블을 `reservation_time`보다 먼저 선언했는데, `reservation`이 `reservation_time`을 FK로 참조하기 때문에 스크립트 실행 시 에러가 발생했다.  
에러 메시지가 SQL 실행 실패로만 나와서 원인을 찾는 데 생각보다 시간이 걸렸다.

### 2. 이번 타임의 학습 전략

- 이전 타임에서 바꾸기로 한 전략(설계 결정 이유 메모)은 이번 타임에서는 크게 적용할 상황이 없었다.
- 시간 관리 API 구현 자체는 예약 API와 구조가 같아서 빠르게 진행됐다.
- 막힌 부분은 에러 메시지를 따라가며 원인을 좁혀나가는 방식으로 해결했다.

### 3. 전략 평가

**효과적이었던 것**: 이전 단계 코드 구조를 그대로 참고해서 빠르게 구현할 수 있었다.

**비효과적이었던 것**: SQL 에러 원인을 찾을 때 에러 메시지만 보고 바로 못 찾았다. DDL 테이블 선언 순서처럼 SQL 레벨의 제약은 먼저 확인하는 습관이 필요하다.

**막힌 것의 종류와 전략의 궁합**: 단순 실수라 전략의 영향은 거의 없었다.

### 4. AI 피드백

> 이번 타임은 구현 자체보다 디버깅에서 시간이 소요된 케이스다.  
> SQL 스크립트 에러처럼 런타임에 터지는 문제는 에러 메시지만으로 원인을 특정하기 어려울 때가 많다.  
> `schema.sql`처럼 초기 실행 스크립트가 있는 경우, 테이블 선언 순서(참조 관계)를 DDL 작성 시점에 체크리스트로 확인하는 습관을 들이면 이런 종류의 실수를 초반에 잡을 수 있다.

### 5. 다음 타임에 바꿀 것

**유지할 것**: 에러 메시지를 단서로 원인을 좁혀나가는 방식.

**바꿀 것**: DDL 작성 시 FK 참조 방향을 먼저 확인하고 테이블 선언 순서를 잡는 습관 추가.
