## 학습 로그 #1

**시간**: 04/28 5:30 ~ 6.00 (약 30분)
**학습 범위**: 1단계 MVC (@RequestBody 역직렬화)

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가?
- [ ] 개념 자체를 모르겠다 
- [x] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (RequestBody의 개념은 알지만 동작 과정을 모르겠다.)
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
```aiignore
이번에 바꾸기로 한 전략은 학습을 시작하기 전에 왜 이 개념을 알아야 하는지를 먼저 생각하고, 학습이 끝난 뒤에는 반드시 회고를 남기는 것이었습니다.
RequestBody는 이미 한 번 사용해본 개념이었지만, 실제로 내부에서 어떻게 동작하는지는 제대로 이해하지 못하고 있었습니다. 특히 JSON이 객체로 변환되는 과정이나 HttpMessageConverter가 어떤 역할을 하는지 설명하려고 하면 흐름이 끊겼습니다.
그래서 이번에는 단순히 사용하는 방법이 아니라, 왜 이런 방식으로 동작하는지까지 이해하는 것을 목표로 잡았습니다.
```
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.
```aiignore
먼저 AI를 통해 RequestBody의 전체 흐름을 질문하면서 제가 모호하게 알고 있던 부분을 정리했습니다.
- DispatcherServlet이 어디서 RequestBody를 처리하는지
- HandlerMethodArgumentResolver와 어떤 관계가 있는지
- HttpMessageConverter가 실제로 JSON을 객체로 바꾸는 과정
- ObjectMapper가 LocalDate 같은 타입을 어떻게 처리하는지

이후에는 Spring 공식 문서와 GitHub 소스를 직접 찾아봤습니다.
특히 MappingJackson2HttpMessageConverter와 ObjectMapper가 연결되는 부분을 보면서 실제로 어디서 변환이 일어나는지 확인했습니다.
단순히 글로 읽는 것이 아니라 클래스 흐름을 따라가면서 이해하려고 했고, 마지막에는 제가 직접 말로 설명할 수 있는지 확인했습니다.

추후 HandlerMethodArgumentResolver의 원천인 ArgumentResolver에 대해 더 깊이 공부해볼 예정입니다.
```
### 3. 전략 평가
- 효과적이었던 것과 그 이유
```aiignore
가장 효과적이었던 것은 문제를 먼저 명확하게 정의한 것이었습니다.
이전에는 그냥 “@RequestBody를 공부해야겠다” 정도였다면, 이번에는 “왜 JSON이 DTO로 변환되는지 설명할 수 있어야 한다”를 기준으로 잡았습니다.
기준이 명확해지니까 어디까지 공부해야 하는지도 자연스럽게 정리되었습니다. 불필요하게 넓게 보지 않고 핵심 흐름에 집중할 수 있었습니다.
또한 공식 문서와 실제 소스코드를 같이 보는 방식도 도움이 많이 되었습니다. 개념만 보면 이해한 것 같지만, 실제 코드 흐름을 보면 놓치고 있던 부분이 바로 보였습니다.
```
- 비효과적이었던 것과 그 
```aiignore
초반에는 너무 세부 구현까지 전부 이해하려고 하면서 시간이 많이 걸렸습니다.
특히 ObjectMapper 내부 구현까지 완전히 파고들려고 하다 보니 지금 필요한 수준을 넘어가는 부분도 있었습니다.
아직도 모든 것을 완벽하게 이해하고 넘어가야 한다는 생각이 남아 있다는 것을 느꼈습니다.
```
- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?
```aiignore
이번처럼 개념은 알고 있지만 코드 흐름이 보이지 않는 문제에서는
문제를 먼저 정의하고, 실제 흐름을 따라가며 검증하는 방식이 잘 맞았습니다.

특히 MVC처럼 내부 동작이 추상적인 개념은 직접 소스코드를 보면서 따라가는 방식이 가장 효과적이었습니다.
```

### 4. AI 피드백
- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고,
  유용했던 제안 1가지 이상 기록
```aiignore
✔ 역할
지금 문제 해결에 필요한 것만 남김
나머지는 의도적으로 버림
“지금 필요한 기준점”만 만든다

3번 핵심 개념 산출이 2번과 역할이 겹친다고 말했습니다. 즉 "지금 필요한 기준점"을 만들고 나머지는 의도적으로 버리는 것으로 
좁히는게 좋다고 피드백을 받게 되었습니다.
```

### 5. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
```aiignore
문제를 먼저 정의하고 학습을 시작하는 방식은 계속 유지합니다.
왜 지금 이 개념을 알아야 하는지가 분명해야 학습 범위를 조절할 수 있고, 핵심에 집중할 수 있기 때문입니다.

또한 공식 문서와 실제 소스코드를 함께 보는 방식도 유지합니다.
단순히 사용법을 익히는 것이 아니라 실제 동작 원리를 구조적으로 이해하는 데 가장 효과적이었습니다.
```
- 바꿀 것과 그 이유
```aiignore
바꿀 것은 핵심 개념을 학습할 때 처음부터 너무 세부 구현까지 파고드는 방식을 줄이는 것입니다. 
핵심 개념을 학습할 때 처음부터 세부 구현까지 깊게 파고드는 방식을 줄입니다.

이전에는 하나의 개념을 이해할 때 내부 동작과 코드까지 모두 이해해야 한다는 기준으로 학습을 진행했습니다.
그 결과 학습 속도가 느려지고 전체 구조보다 세부 구현에 집중하게 되는 문제가 발생했습니다.

특히 처음 접하는 기술에서는 세부 구현보다 전체 구조와 역할을 먼저 이해하는 것이 더 중요하다는 점을 확인했습니다.

따라서 앞으로는
전체 흐름 → 각 구성 요소의 역할 → 필요 시 세부 구현
순서로 접근 방식을 변경합니다.

이렇게 하면 학습 우선순위가 명확해지고 이후 세부 내용을 학습할 때도 전체 구조 안에서 빠르게 연결할 수 있습니다.
```

### 단계 전환 체크
- 이전 단계에서 쓴 접근법을 그대로 쓸 것인가, 바꿔볼 것인가?
``` 
큰 방향(문제 정의 → 흐름 중심 이해 → 공식 문서 + 소스코드 확인)은 유지합니다.

다만 세부 구현을 처음부터 깊게 파고드는 방식은 줄이고,
역할과 전체 흐름을 먼저 이해한 뒤 필요한 순간에 세부 구현으로 들어가는 방식으로 조정합니다.
```
- 바꾼다면 왜? 바꾸지 않는다면 왜?
```aiignore

조정하는 이유

다만 세부 구현까지 처음부터 깊게 들어가는 방식은
현재 학습 단계에서는 효율 대비 시간이 많이 소모되고,
전체 구조를 먼저 이해해야 하는 흐름을 방해하는 문제가 있었습니다.

특히 MVC, Spring 내부 구조, DB, 네트워크처럼
추상도가 높은 개념에서는 전체 구조 → 세부 구조 순서가 더 효과적이라는 결론을 얻었습니다.
```
- 이전 단계에서 발견한 "나에게 맞는 전략"이 새 단계에서도 통할 것 같은가?
```aiignore
통할 가능성이 높습니다.
이 전략의 핵심은 “깊이”가 아니라 “순서”에 있기 때문입니다..

먼저 왜 필요한지 정의하고
전체 흐름을 잡고
필요한 순간에만 내부 구현으로 들어가는 구조

이 방식은 MVC, Spring 내부 구조, DB, 네트워크처럼
추상도가 높은 개념을 다룰 때 오히려 더 효과적입니다.
```