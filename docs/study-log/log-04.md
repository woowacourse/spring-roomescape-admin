## 학습 로그 #4

**시간**: 04/29 21:00 ~ 21:30 (약 30분)
**학습 범위**: 3단계 시간 관리 (기본 시간 슬롯 초기화, 연관관계가 있을 때의 post/get/delete)

### 1. 막힌 것의 종류

이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)

- [ ] 개념 자체를 모르겠다
- [ ] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다
- [x] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (꼭 LMS에 올라온 구조로 구현해야할까? 외래키 제약, 테스트 등)
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?

```
# 바꾸기로 한 전략
- 여러 주제가 동시에 떠오르면 바로 해결하려 하지 않고, 먼저 문제를 분리한다.
- 분리한 문제 중 지금 목표에 직접 필요한 것부터 하나씩 해결한다.
- 테스트 통과를 위한 최소 정책과 실제 서비스 정책처럼, 성격이 다른 문제는 섞어서 판단하지 않는다.

# 실행 여부
- O
```

- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

```
# 실제 학습 과정
- 이전 학습에서는 save 정책을 고민하다가 기본 데이터, 중복 요청, 테스트 전제, 실제 서비스 정책까지 한꺼번에 생각했다.
  그래서 먼저 문제를 나눴다.

  1. 기본 시간 슬롯을 어떻게 초기화할 것인가
  2. /times POST는 중복 요청을 어떻게 처리할 것인가
  3. /reservations POST는 timeId를 어떻게 참조할 것인가
  4. /times DELETE는 참조 중인 시간이 있을 때 어떻게 처리할 것인가
  5. 테스트가 기대하는 상태와 실제 서비스 정책을 어떻게 분리할 것인가

- 이번에는 모든 문제를 한 번에 해결하려 하지 않고, 먼저 테스트 통과에 직접 필요한 문제부터 보았다.
- 고정된 테스트에서는 reservation 생성 시 timeId=1이 필요했다.
  따라서 reservation_time에 id=1인 데이터가 존재해야 한다는 것을 먼저 확인했다.
- 하지만 /times POST 이후 size가 1이어야 하는 테스트도 있었기 때문에,
  기본 시간 슬롯을 무작정 24개 넣으면 테스트와 충돌한다는 점을 다시 확인했다.
- 그래서 이번 단계에서는 "기본 시간 슬롯 24개를 반드시 넣어야 한다"는 생각을 잠시 내려놓고,
  현재 테스트가 요구하는 최소 상태를 먼저 만족시키는 방향으로 정리했다.
- 동시에 data.sql은 Repository를 거치지 않기 때문에 중복 insert 문제는 Repository에서 해결할 수 없고,
  테스트용 초기 상태를 분리해야 한다는 점을 인지했다.
- /times POST의 save 정책도 실제 서비스 정책까지 확정하려 하지 않았다.
  우선 현재 요구사항과 테스트를 만족하는 최소 동작을 기준으로 판단했다.
- 이렇게 문제를 하나씩 나누어 보니, 이전보다 훨씬 빠르게 어디를 수정해야 하는지 보였다.

# 후기
- 이전에는 하나의 문제가 생기면 관련된 모든 설계 문제를 한 번에 해결하려고 했다.
  이번에는 떠오른 주제를 먼저 적고, 지금 목표에 필요한 것부터 하나씩 처리했다.
- 이 방식이 생각보다 효과적이었다.
  문제를 나누고 나니 실제로 지금 해결해야 하는 것과 나중에 정리해도 되는 것이 분리되었다.
- 덕분에 3시간 동안 정리하지 못했던 문제를 30분 정도 안에 해결 방향까지 잡을 수 있었다.
```

### 3. 전략 평가

- 효과적이었던 것과 그 이유

```
# 효과적이었던 것
- 여러 주제가 동시에 떠올랐을 때 바로 해결하지 않고, 먼저 문제를 분리한 것
- 테스트가 요구하는 최소 상태와 실제 서비스 정책을 분리해서 본 것
- 지금 목표에 직접 필요한 문제부터 하나씩 해결한 것

# 이유
- 이전에는 save 정책을 고민하면서 기본 데이터, 중복 요청, 테스트 전제, 실제 서비스 정책까지 동시에 생각했다.
- 이번에는 문제를 분리하면서 지금 해결해야 할 범위가 명확해졌다.
- 특히 테스트가 원하는 것은 "완벽한 서비스 정책"이 아니라 "현재 단계에서 기대하는 동작"이라는 점을 먼저 확인할 수 있었다.
- 그 결과 불필요하게 깊어지는 고민을 줄일 수 있었다.
```

- 비효과적이었던 것과 그 이유

```
# 비효과적이었던 것
- 처음에는 여전히 LMS에 올라온 구조나 정답처럼 보이는 구조를 기준으로 맞춰야 한다고 생각한 것
- 그래서 "이 구조가 꼭 맞는가?"보다 "이 구조를 어떻게든 유지해야 하는가?"에 잠깐 묶였다.

# 이유
- 요구사항과 테스트가 우선인데, 예시 구조를 정답처럼 받아들이면 판단이 흐려진다.
- 특히 DB 연관관계나 기본 데이터 초기화처럼 구현 방식이 여러 개인 문제는,
  먼저 현재 요구사항과 테스트가 원하는 동작을 확인해야 했다.
```

- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?

```
# 막힌 것의 종류와 전략 궁합
- 이번에는 "코드는 돌아가는데 이게 맞는지 모르겠다"는 유형의 막힘이었다.
- 이런 문제는 단순히 검색으로 답을 찾기보다, 선택지를 나누고 기준을 세우는 방식이 더 잘 맞았다.
- 여러 주제를 분리하고 하나씩 처리하는 전략이 이번 문제와 잘 맞았다.
- 특히 정책성 판단이 필요한 문제에서는 한 번에 정답을 찾으려 하기보다,
  현재 목표에 필요한 최소 판단부터 하는 것이 더 효과적이었다.
```

### 4. AI 피드백

- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

```
# 나의 전략 분석
- 이전에는 문제가 복잡해지면 관련된 주제를 한 번에 다 해결하려는 경향이 있었다.
- 이번에는 기본 데이터, 중복 저장, 테스트 전제, 실제 서비스 정책이 동시에 떠올랐지만,
  먼저 문제를 나누고 우선순위를 정하는 방식으로 접근했다.
- 그 결과 고민의 범위가 줄었고, 해결 순서가 명확해졌다.

# AI 학습 전문가의 피드백
- 여러 주제가 동시에 떠오를 때는 바로 해결책을 찾지 말고 먼저 분리해야 한다.
- 특히 정책이 필요한 문제는 다음 두 가지를 구분해야 한다.
  1. 지금 목표를 만족시키기 위한 최소 정책
  2. 실제 서비스에서 장기적으로 유지할 정책
- 모든 정책을 한 번에 확정하려고 하면 학습 범위가 커진다.
- 먼저 현재 목표에 필요한 문제를 해결하고, 나머지는 다음 학습의 입력으로 남기는 것이 더 효율적이다.

# 유용했던 제안
- 팝업된 주제를 먼저 적고, 지금 처리할 것과 나중에 처리할 것을 나누는 방식이 유용했다.
- 이번에는 다음처럼 나누는 것이 도움이 되었다.
  - 지금 처리할 것: 테스트가 요구하는 초기 상태와 저장 결과
  - 나중에 처리할 것: 실제 서비스에서 중복 시간 요청을 어떻게 정책화할지
  - 별도로 볼 것: data.sql 초기화와 API 저장 정책의 책임 분리

# 적용 결과
- 문제를 분리하니 이전보다 훨씬 빠르게 해결 방향을 잡을 수 있었다.
- 앞으로도 여러 주제가 동시에 떠오르면 바로 구현하지 않고, 먼저 분리한 뒤 하나씩 타파하는 방식으로 진행할 것이다.
```

### 5. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

```
# 유지할 것
- 여러 주제가 동시에 떠오르면 먼저 분리하고, 지금 목표에 필요한 것부터 하나씩 해결하는 방식
- 테스트 통과를 위한 최소 정책과 실제 서비스 정책을 구분해서 보는 방식
- 예시 구조를 그대로 따르기보다 요구사항과 테스트가 실제로 요구하는 동작을 먼저 확인하는 방식

# 이유
- 이번 방식은 복잡한 문제를 빠르게 정리하는 데 효과적이었다.
- 특히 DB 연관관계처럼 여러 문제가 같이 따라오는 상황에서 학습 범위를 줄이는 데 도움이 되었다.
- 요구사항, 테스트, 실제 정책을 분리하니 판단 기준이 더 명확해졌다.
```

- 바꿀 것과 그 이유

```
# 바꿀 것
- 문제를 나눈 뒤에도, 각 문제마다 "지금 해결할 것인지, 기록만 할 것인지"를 더 명확히 표시한다.
- 구현 전에 짧게라도 문제 목록과 우선순위를 적고 시작한다.

# 이유
- 문제를 분리하는 것만으로도 도움이 되었지만,
  기록하지 않으면 다시 머릿속에서 여러 주제가 섞일 수 있다.
- 다음에는 문제를 만났을 때 아래처럼 먼저 정리하고 시작한다.

  1. 지금 바로 해결할 문제
  2. 이번 단계에서는 기록만 할 문제
  3. 요구사항이 더 명확해지면 다시 볼 문제

- 이렇게 하면 학습 중간에 다른 주제로 깊게 빠지는 일을 줄일 수 있을 것 같다.
```

### 단계 전환 체크

- 이전 단계에서 쓴 접근법을 그대로 쓸 것인가, 바꿔볼 것인가?

```
# 결론
- 접근법은 유지한다.
- 다만 여러 주제가 동시에 떠오르면 바로 해결하지 않고, 먼저 문제를 분리한 뒤 진행한다.

# 이유
- 구현 중심으로 빠르게 진행하는 방식은 여전히 효과적이었다.
- 다만 이번 단계에서는 save 정책을 고민하다가 기본 데이터, 중복 요청, 테스트 전제, 실제 서비스 정책까지 한꺼번에 이어졌다.
- 따라서 기존 접근법은 유지하되, 문제를 나누고 우선순위를 정하는 과정이 필요하다고 판단했다.
```

- 바꾼다면 왜? 바꾸지 않는다면 왜?

```
# 바꾸지 않는 이유
- 목표 중심으로 필요한 만큼만 학습하는 방식은 학습 속도와 집중도를 유지하는 데 효과적이다.
- 모든 문제를 처음부터 완벽하게 이해하려는 방식보다 현재 단계에서는 더 효율적이다.

# 바꾸는 부분
- 여러 문제가 동시에 떠오르면 먼저 분리한다.
- 분리한 문제 중 지금 목표에 직접 필요한 것부터 하나씩 해결한다.
- 테스트 통과를 위한 최소 정책과 실제 서비스 정책처럼 성격이 다른 문제는 섞어서 판단하지 않는다.

# 바꾸는 이유
- 이번에는 하나의 save 정책을 고민하다가 학습 범위가 너무 넓어졌다.
- 문제를 먼저 나누고 우선순위를 정하니, 해결해야 할 것과 나중에 다뤄도 되는 것이 명확해졌다.
```

- 이전 단계에서 발견한 "나에게 맞는 전략"이 새 단계에서도 통할 것 같은가?

```
# 결론
- 통한다.

# 이유
- 필요한 만큼 학습하고 직접 구현하는 방식은 여전히 잘 맞는다.
- 하지만 DB 연관관계, 테스트 전제, 저장 정책처럼 여러 주제가 연결되는 문제에서는 바로 깊게 들어가면 범위가 커진다.
- 따라서 먼저 문제를 나누고, 현재 목표에 필요한 것부터 해결하면 기존 전략을 더 안정적으로 사용할 수 있다.

# 기대 효과
- 단기: 지금 해결해야 할 문제에 집중
- 장기: 테스트 통과용 판단과 실제 서비스 정책 판단을 구분하는 습관 형성
```
