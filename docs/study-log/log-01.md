## 학습 로그 #1

**시간**: 04/28 14:10 ~ 16:30 (약 80분)
**학습 범위**: 1단계 MVC (DTO validation, ResponseEntity와 REST API)

### 1. 막힌 것의 종류

이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)

- [ ] 개념 자체를 모르겠다
- [x] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (@Size의 Validate 테스트)
- [x] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (ResponseEntity와 RestController등)
- [ ] 기타: ___

### 2. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?

```
# 바꾸기로 한 전략 (추가하기로 한 전략)
- 구현을 하다가 모르는 것이 있으면 모르는 것이 뭔지를 정의하고,
  모르는 것을 알게되는 순간까지만 서칭한다.
- 막히는 부분을 학습하며 만난 키워드가 있다면, 잠깐 스스로 생각해보는 시간을 가진다.

# 실행 여부
- O
```

- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

```
# 실제 학습 과정
- 예를 들어 DTO에서 name field의 길이를 validation할 떄, @Size를 사용하는 것을 알고있었다.
  하지만 DTO를 테스트하고싶어졌고, 평소하던대로 테스트를 assertThrownBy로 구현했다.
  @Size의 max속성은 어떤 타입의 오류를 내는지 궁금했고 검색했다.
  목적을 해결했기 때문에 그 예외의 내부나 @Size의 동작은 정확하게 찾아보지않았다.
- 여전히 assertThrownBy로는 오류가 잡히지 않았다.
  일반 new 생성자로는 @Size가 동작하지 않는다고 한다.
  대신 Spring MVC의 Controller Layer에서만 동작한다는 사실을 알게되었다.
  Controller Test에서 검증하니 400으로 에러가 난다는 것을 잘 테스트할 수 있었다.
- 나중에 생각해보니 Controller의 매개변수에 @Valid를 선언하기 떄문에 해당 경로가 아닌 DTO생성은 검증되지 않는다는 것을 알게 되었다.
  
# 후기
- 학습을 대충한다는 것이 아니다. 
  목적을 명확히하고 목적에서 벗어나는 것은 기록해두고 나중에 찾아본다.
  지금 이해가 안되는 것도 나중에는 더 자연스럽게 이해할 수 있다.
```

### 3. 전략 평가

- 효과적이었던 것과 그 이유

```
# 효과적이었던 것
- 처음 궁금했던 것이 해결되면 그만 찾아보고 진행하는 것
- 깊이있는 학습은 다음에 만났을 때 또는 더 학습하고 싶을 때 하기

# 이유
- 스프링은 모르는 것 투성이라고 생각한다.
  전부 알고싶어하는 마음은 좋지만 효율이 떨어질 것이다.
- 일단 활용을 할 수 있음을 목표로 두고 학습을 하는 것이 좋다고 생각한다.
  나중에 다시 만나면 깊이를 더해가면 된다.
```

- 비효과적이었던 것과 그 이유

```
# 비효과적이었던 것
- 막히는 부분을 학습하며 만난 키워드가 있다면, 잠깐 스스로 생각해보는 시간을 가진다.

# 이유
- 잠깐 생각하다가 살짝 깊은 생각에 빠진다.
  예를 들어 @Size validation에 실패했을 때 던지는 MethodArgumentNotValidException 타입의 오류를 어떻게 처리하면 좋은가를 생각해버렸다.
  GlobalExceptionHandler 구현에 대한 생각까지도 이어졌다.
  테스트를 하는 것이 목적이었는데 말이다.
```

- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?

```
# 막힌 것의 종류와 전략의 궁합
- 좋았다고 생각한다. 내가 원하는 답을 알면 계속 진행했기 때문에 속도도 빨랐고 머릿속에도 잘 남아있다.
- 비효과적이었던 전략도 방향을 잘못잡아서 그렇고 좋은 방식이라고 생각한다.
  대신 생각이 너무 깊어진다 싶으면 그만하고 다시 서칭하는 의식적인 연습을 해야겠다.
```

### 4. AI 피드백

- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고,
  유용했던 제안 1가지 이상 기록

```
# 나의 전략 분석
- 예상) 목적 중심 → 필요한 만큼만 학습 → 다음에 깊이 확장
- 실제) 목적 해결 → 추가 의문 발생 → 그 의문을 즉시 해결하려고 함 → 깊이 과몰입
- 전략 자체 문제가 아니고 전략의 종료 조건이 불명확하다는 문제이다.

# 해결 방안 제시
- 문제 해결에 직접적으로 도움을 주는 의문이 아니면 기록만하고 종료해보는 것
- 꼬리 질문은 레벨을 두 가지로 분리
    Q0: @Size 검증 테스트는 어떻게 하지? (본래 목적)
    Q1: MethodArgumentNotValidException 처리 어떻게 하지?
    Q2: GlobalException을 처리해야하나? (의문 종료)
```

### 5. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

```
# 유지할 것
- 구현을 하다가 모르는 것이 있으면 모르는 것이 뭔지를 정의하고,
  모르는 것을 알게되는 순간까지만 서칭한다.
  
# 이유
- 하나의 목적을 이룬다는 학습법을 잘 실천할 수 있게 도와주었다.
```

- 바꿀 것과 그 이유

```
# 바꿀 것
- 막히는 부분을 학습하며 만난 키워드가 있다면, 잠깐 스스로 생각해보는 시간을 가진다.

# 이유
- 잠깐 생각한다는 추상적인 단어를 고친다.
  최대 두 단계정도로만 생각의 깊이를 제한한다. (이미 원래 목적은 해소되었다는 가정)
```

### 단계 전환 체크

- 이전 단계에서 쓴 접근법을 그대로 쓸 것인가, 바꿔볼 것인가?

```
# 결론
- 접근법은 유지한다.
- 다만 "생각의 깊이 제한"은 명확하게 설정한다.

# 이유
- 현재 전략은 학습 속도와 이해도를 동시에 확보하는 방향으로 잘 작동하고 있다.
- 문제는 전략 자체가 아니라, 적용 과정에서 경계가 흐려지는 부분이었다.
- 따라서 새로운 전략을 도입하기보다는, 기존 전략을 정교하게 다듬는 것이 더 효율적이라고 판단한다.
```

- 바꾼다면 왜? 바꾸지 않는다면 왜?

```
# 바꾸지 않는 이유
- 목적 기반 학습, 필요한 만큼만 학습, 다음에 확장하는 구조는 스프링처럼 범위가 넓은 영역에서 매우 적합하다고 생각한다.
- 또한 실제로 속도와 이해도 측면에서 효과를 확인했다.

# 바꾸는 부분
- "잠깐 생각한다"는 전략을 구체화한다.
    질문 레벨 분리
    생각 깊이를 2단계로 제한
    해결과 무관한 질문은 보류

# 바꾸는 이유
- 현재 비효율의 원인은 탐색 범위 확장이다.
- 이를 제어하지 않으면 이후 더 복잡한 개념에서 학습 속도가 급격히 떨어질 가능성이 있다.
```

- 이전 단계에서 발견한 "나에게 맞는 전략"이 새 단계에서도 통할 것 같은가?

```
# 결론
- 네.

# 이유

- 스프링 학습은 구조적으로 하나의 개념을 이해하면 그 주변 개념들이 연쇄적으로 따라온다.
- 현재 전략은 "목적(Q0)을 먼저 해결하고, 나머지 질문(Q1, Q2)은 분리하는 방식"이기 때문에, 확장 구조를 통제하면서 학습할 수 있다.

# 리스크
- 개념 간 연결성이 강해질수록 Q1, Q2 질문이 빠르게 증가할 것 같다.

# 기대 효과

- 단기: 현재 목표(Q0)에 집중하여 학습 속도 유지
- 중기: Q1을 통해 점진적으로 개념 연결
- 장기: Q2를 기반으로 구조적인 이해 축적 (나선형 확장)
```
