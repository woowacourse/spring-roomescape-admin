## 학습 로그 #3

**시간**: 04/29 20:10-22:45 (약 2시간 30분)

**학습 범위**: 3단계 시간 관리

### 1. 막힌 것의 종류
이번에 막힌 것은 어떤 종류의 어려움이었는가? (해당하는 것에 체크)
- [ ] 개념 자체를 모르겠다 (예: "스프링 빈이 뭔지 모르겠다")
- [ ] 개념은 알겠는데 코드로 어떻게 쓰는지 모르겠다 (예: "JdbcTemplate 문법을 모르겠다")
- [ ] 코드는 돌아가는데 이게 맞는 건지 모르겠다 (예: "계층 분리를 이렇게 해도 되나?")
- [x] 기타: 요구사항을 제대로 읽지 않았다가, Time을 Reservation 내부에 필드로 둘 지 timeId만 둘지 고민하느라 잠시 막혔었다. 요구사항을 읽고 나서는 ReservationTime 타입 필드로 두었다.

### 2. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
  - `각 스텝마다 데드라인을 잡는다.`를 AI 피드백에 따라 `체크포인트 방식`으로 변경하기로 했다.
  - 하지만 이번 단계에서는 새로운 기술을 배우는 부분은 없었기에 크게 사용되진 않았지만, 그래도 피드백대로 시간을 명시하진 않았다.
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.
  - 아래 메모와 같이 진행했다.
  ```
  3단계: 시간 관리
  구현
  - 스키마 변경/추가
  - Reservation 객체 time 필드 timeId로 변경
      - ReservationTime 타입으로 넣어야 하는 거였음
  - TimeController에 시간 추가/조회/삭제 API 구현
  - 테스트 추가 및 성공 확인 (+테스트 오류 반영)
  
  고민
  - Reservation 내에 Time 한번에 둘지 id만 갖게 할지
      - 예약 조회 할 때, 조인해서 response만 담으면 됨
      -> 요구사항에 Time으로 바로 두게끔 나와있었음
  ```

### 3. 전략 평가
- 효과적이었던 것과 그 이유
  - `해야 할 일을 스텝별로 나누고, 글로 명시해두기`
    - 이렇게 나눴기에 계속해서 집중이 엇나가지 않도록 붙잡을 수 있었다.
- 비효과적이었던 것과 그 이유
  - 이번엔 크게 없었다.
- 막힌 것의 종류(1번)와 전략의 궁합은 어땠는가?
  - 이번에 요구사항을 제대로 안 읽어서 막혔던 부분과는 별개였다고 생각한다.

### 4. AI 피드백
- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고,
  유용했던 제안 1가지 이상 기록
```
구현에 들어가기 전, 설계 고민을 시작하기 전에 “요구사항 고정점”을 먼저 표시한다.
```

### 5. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
  - `해야 할 일을 스텝별로 나누고, 글로 명시해두기`
    - 앞에서의 이유와 같이, 집중을 되찾아 올 수 있는 좋은 방법이다.
- 바꿀 것과 그 이유
  - AI 피드백대로 요구사항 고정점은 먼저 챙겨두려고 한다.

### 단계 전환 체크
- 이전 단계에서 쓴 접근법을 그대로 쓸 것인가, 바꿔볼 것인가?
  - 그대로 쓰되 `설계 고민을 시작하기 전에 “요구사항 고정점”을 먼저 표시`를 더할 것이다.
- 바꾼다면 왜? 바꾸지 않는다면 왜?
  - 요구사항을 발견하지 못 해서 고민하느라 시간을 소모했기 때문이다.
- 이전 단계에서 발견한 "나에게 맞는 전략"이 새 단계에서도 통할 것 같은가?
  - 그렇다
