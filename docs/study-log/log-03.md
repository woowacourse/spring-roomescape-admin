## 학습 로그 #N

**시간**: 4/30 00:00 ~ 01:00 (약 60분)
**학습 범위**: 3단계 시간 관리

### 1. 막힌 것의 종류
- 전반적으로 복습 성격이 강해서 크게 막히는 부분은 없었다.
- RowMapper 안에서 객체를 쓰듯이 접근하려다 막혔다.
  - DB는 컬럼 단위로 분해되어 오고, Java는 객체로 계층이 있다는 것을 다시 체감했다.
- 도메인, Controller, DAO 사이 계층 분리를 다시 생각하게 됐다. 다음 단계에서 깊게 다룬다.

### 2. 이번 타임의 학습 전략
- Time 객체 생성, JOIN 쿼리 등 이전에 익힌 것들은 바로 작성했다.
- 애매한 부분만 AI와의 대화를 통해 채웠다.

### 3. 전략 평가
**효과적이었던 것**
복습 단계라 범위를 더 좁힐 수 있었다. 막히는 부분만 골라서 AI에게 질문하는 방식이 시간 효율이 좋았다.

### 4. AI 피드백
- 복습 단계일수록 의도적 연습이 더 중요하다. 쉽게 느껴지는 순간이 바로 comfort zone이기 때문이다. 이번에 JOIN 쿼리 실수, RowMapper 착각처럼 막힌 지점이 있었는데, 그 지점을 그냥 해결하고 넘어간 것인지, AI에게 설명하고 왜 그런 실수를 했는지 피드백까지 받았는지가 차이를 만든다.
다음 복습 단계에서는 "쉬웠다"고 느껴지는 순간에 오히려 하나를 골라 더 깊게 설명해보는 것을 권한다. 쉽다는 느낌이 진짜 이해인지 확인하는 기회다.

### 5. 다음 타임에 바꿀 것
**유지할 것**
- 학습 목표를 먼저 정하는 것
- 범위를 좁혀 과제 완성 수준으로만 이해하는 것
- deep dive 목록을 열어두는 것
- 세션 끝에 AI에게 설명하고 즉각 피드백 받는 것


---

## 학습 과정

### 1. 학습 목표
"3단계를 배우는 이유 → 도메인 간 의존 관계가 생겼을 때 DB와 Java 코드가 어떻게 연결되는지 이해하기 위해"

### 2. 선 이해

Time 객체, JOIN 쿼리 등 이전에 익힌 것들은 바로 작성할 수 있었다.

### 3. AI에게 설명하고 피드백 받기

### 4. 정리하기

과제를 완성한 후 계층 분리와 DTO 필요성을 중심으로 전체 흐름을 다시 정리했다.

### 5. 다음 deep dive

1. JOIN 종류 — INNER JOIN, LEFT JOIN, RIGHT JOIN 차이. 예약은 있는데 시간이 삭제된 경우 어떻게 조회할 것인가
2. 외래키 + 관계 — 다대일, 다대다 관계.
3. 계층 분리 — Controller → Service → Repository 구조가 왜 필요한가. 지금 Controller가 DAO를 직접 쓰는 문제
4. 쿼리와 도메인의 관계 — DB 구조가 바뀌면 Java 코드 어디까지 영향을 받는가