spring:
  profiles:
    active: test

  h2:
    console:
      enabled: true
      path: /h2-console
  # DB connection setting
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password: password
  # DB initialization
  sql:
    init:
      mode: embedded
      schema-locations: classpath:schema.sql
