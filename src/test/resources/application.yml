spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    #    show-sql: true
    properties:
      hibernate:
        format_sql: true
        hbm2ddl:
          auto: none

jwt:
  secret: "testscretkeytestscretkeytestscretkeytestscretkey"
  expiration: 1800000
  expirationInSeconds: 1800
